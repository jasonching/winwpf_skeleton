<UserControl x:Class="MyApp.Wpf.GameOfLife.GameBoard.GameBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:l="clr-namespace:MyApp.Wpf.GameOfLife.Observer"
             xmlns:c="clr-namespace:MyApp.Wpf.GameOfLife.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <c:UnitBackgroundConverter x:Key="unitBackgroundConverter" />
    </UserControl.Resources>
    <Grid>
        <ItemsControl x:Name="Units" l:SizeObserver.Observe="True"
                      l:SizeObserver.ObservedWidth="{Binding BoardWidth, Mode=OneWayToSource}"
                      l:SizeObserver.ObservedHeight="{Binding BoardHeight, Mode=OneWayToSource}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl> 
    </Grid>
</UserControl>
